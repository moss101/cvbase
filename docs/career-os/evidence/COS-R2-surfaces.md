# R1/R2 surfaces — per-task verification record (COS-009 … COS-030)

21 September 2026 · Vite dev server (`CVBASE_ENV_FILE=.env.cvbase.localstack`) against the local Supabase stack; Chromium desktop 1024×768, mobile 375×812 / 320×640, light and dark; real passwordless sign-in and real database writes. Live AI calls were not run (paid, opt-in) — every AI path was exercised through its honest unavailable/abstain state. Nothing touched the hosted project.

This note records, per task, what exists, which automated checks cover it and which live journey exercised it. Journey narratives are in [COS-018-r1-qualification.md](COS-018-r1-qualification.md); mobile/native/a11y/localization in [COS-032-033-experience-performance.md](COS-032-033-experience-performance.md); the frontier modules in [COS-035-037-frontier-logic.md](COS-035-037-frontier-logic.md). Final automated totals: `npm run typecheck` clean; `npx vitest run` 63 files / 661 tests; `deno test` 272 passed; `deno check` on all 18 function entries ([final-verification.log](final-verification.log)).

| Task | What exists | Automated (passing) | Live (this session) |
| --- | --- | --- | --- |
| COS-009 Import and review canonical career facts | `services/careerOs/careerFacts.ts` (candidates from resume/profile, fingerprints, identity keys, period-aware reconciliation, `impactOfChange`), `factRepo.ts` (+ references), Career → Evidence/Facts/Achievements views, `ImportPanel`, `ReviewQueue`, `FactEditor`, `VerifyDialog` | `careerFacts.test.ts` 14, `repos.core.test.ts` 25, `career.test.tsx` 8, `mappers.test.ts` 23 | J02: 10 candidate facts + 1 `user_confirmed` profile fact from migration; "Confirm all 9" → `user_confirmed`/`reviewed`; `career_evidence_confirmed ×9`, `career_import_reviewed ×1`. J01: paste → 10 candidates, 0 contradictions, review persisted across reload. Unsupported claims stay `candidate` until confirmed. |
| COS-010 Reusable goals and primary-goal selection | `goalRepo.ts` (+ revisions trigger), `GoalForm`, `GoalsView`, `CareerGoalCard`, `ScenarioPanel` entry | `repos.core.test.ts`, `careerContext.test.ts` 12 (goal in context), `career.test.tsx` | J02: structured goal created, primary set, revision 2 after edit; `SET_GOAL` completed from the durable goal (not from opening the route); decision-time `goal_snapshot` captured on application start. |
| COS-011 Persist imported opportunities and saved views | `opportunityRepo.ts`, `ImportOpportunityDialog` (heuristic title/company/location, editable), `OpportunityList` with saved views, `OpportunityDetail`, freshness/ranking helpers | `opportunities.test.tsx` 9, `repos.core.test.ts`, `navigationRoutes.test.ts` 22 (query views) | J02: pasted JD with an embedded instruction → 6 requirements extracted, injected line not extracted; `opportunity_saved`; Watch does not stale the analysis (input fingerprint). Migration kept 3 tracker rows as opportunities with their application ids. |
| COS-012 Extend application persistence and start flow | `career_start_application` RPC (idempotency key, attempt, reapply), `applicationRepo.ts`, `useStartApplication`, stage/legacy-status trigger, `ApplicationList` | `repos.execution.test.ts` 15, `trackerRepo.test.ts` 14 (legacy compatibility), `career-os-migration-smoke.sh` (idempotent start ×2, reapply attempt 2) | J02: start → `preparing`/`wishlist`, attempt 1, key `start:<opportunity>`; repeated start returns the same row; legacy tracker still reads the row. |
| COS-013 PRISM binding | see [COS-013-prism-binding.md](COS-013-prism-binding.md); wizard `binding` prop + `llm_unavailable`/`checkpoint_degraded` copy | prism-tailor Deno 72 (21 binding), `PrismWizard.test.tsx` 12, `prismService.test.ts` 10, `prismRepo.test.ts` 4 | J02 step 6: bound run `failed/llm_unavailable`, `source_resume_revision = 1`, charged then released (`ai_actions` 0), `prism_run_id` set, readiness CV item `blocked` with retry. |
| COS-014 Shared action rules and lifecycle | `careerActions.ts` (rules-1.0.0, ranking, `reconcileDurableCompletions`), `actionRepo.ts` (dedupe, expiry, 23505 tolerated) | `careerActions.test.ts` 23, `repos.queue.test.ts` 14 | J02/J01: rule actions with version, reasons and evidence chips; completion only from owned results (`completion_source = durable_receipt`); stale rule rows expire; concurrent loads tolerated. |
| COS-015 Today on real context and actions | `TodaySpace`, `useTodayData`, `OrientationHeader`, `ActionQueue`, `CareerPulse`, `ActiveCampaigns`, `RecentActivity`, `InsightsPanel`, `IntroductionCard`, `ClaimDraftDialog` | `today.test.tsx` 7, `careerEvents.test.ts` 10 | J02: real orientation counts, three ranked actions, checklist pulse, empty campaigns without auto-creation, insights "inside the observation window"; J01: one non-CV action (J10); J03: anonymous claim dialog. Date localised with `Intl` (es checked). |
| COS-016 First application workspace with the existing CV engine | `ApplicationWorkspace` + 9 sections (`CvSection` opens the existing builder/PRISM by document id; analysis, cover letter, questions, LinkedIn, networking, notes, interview, activity), `ReadinessChecklist`, `SubmissionDialog`, `OutcomeDialog`, `FailureNotice` | `application.test.tsx` 8, `campaignReadinessOutcomes.test.ts` 8 (readiness), `usePersistence.test.tsx` 18 (document identity) | J02 steps 5–9; J05 two applications coexist with scoped PRISM lookup; rails stack below 1280 px (fix). |
| COS-017 Product events and measurements | `careerEvents.ts` (envelope, redaction, `isForbiddenPayloadKey`), `eventRepo.ts` | `careerEvents.test.ts` 10 | Events observed in `career_events` for every step above; `career_action_completed` restored after the redaction fix; no free text or compensation values in payloads. |
| COS-019 Career evidence and progression surfaces | `CareerSpace` (Overview, Profile, Facts, Evidence, Achievements, Goals), `CareerTimeline`, `AchievementCard`, `EvidenceBadge`, `ImpactBanner` | `career.test.tsx` 8, `primitives.test.tsx` 18 | J02: evidence review, timeline from confirmed facts. J10 (live, `careeros-new`): Today offered "Capture a recent achievement" (0 recorded, linked to the primary goal) → Begin → Career → Achievements form (title, period, metric 40 %, linked role, cited evidence, narrative) → fact `kind = achievement`, `user_confirmed`, `parent_fact_id` set, payload `{metric, unit, period}`; on the next Today load the action completed with `completion_source = durable_receipt` → `{kind:'fact', id, revision}` and `career_action_completed` was emitted (completion rule added during the drill). 320 px single column. |
| COS-020 Qualification vs career-direction fit | `careerFit.ts` (fit-1.0.0, `analysisInputFingerprint`, `isAnalysisStale`), `analysisRepo.ts`, `FitPanel`, `FitBreakdown` | `careerFit.test.ts` 11, `opportunities.test.tsx`, evaluation harness goal-fit ×4 / qualification ×2 | J02 step 4: 2/2/2/0 with cited facts and confirmation state; direction fit separate; "Pay not stated" unknown; ATS 72/100 shown as a distinct labelled signal; `opportunity_fit_reviewed`. |
| COS-021 Campaign milestones and application board | `campaignRepo.ts`, `campaignFunnel.ts`, `CampaignList/Detail/Form`, `ApplicationBoard` (Earlier/Later, no drag requirement), `stages.ts` | `campaigns.test.tsx` 6, `campaignReadinessOutcomes.test.ts` (funnel), `navigationRoutes.test.ts` (`?view=` kept) | Live (QA account): campaign created explicitly toward the primary goal (`campaign_created {hasGoal:true, milestones:0}`); "Assign existing application" attached the saved Fabrikam application; board "Later" moved it Target roles → Preparing (`stage = preparing`, legacy `status = wishlist` via the trigger, revision 4, funnel Preparing 1, banner "moved by you"); Undo returned it to `saved` (revision 5). Detail keeps `?view=` on reload (fix). Heading layout fix: wide pane actions now wrap beneath the text at the 1024 px shell width. |
| COS-022 Persist application preparation artifacts | `artifactRepo.ts`, `ArtifactEditor`, per-section artifacts (`role_analysis`, `cover_letter`, `employer_question`, `linkedin`, `networking_note`, `note`, `interview_story`, `submission`) with revisions and fact references | `repos.execution.test.ts`, `application.test.tsx` | Artifact sections rendered with their empty/unavailable states in J02 (AI-backed generation shows the unavailable state uncharged). Saving artifacts with revisions and fact references is covered by `repos.execution.test.ts` / `application.test.tsx`; no artifact was authored by hand in this session. |
| COS-023 Track factual changes across generated assets | `impactOfChange` + fact references, `EditImpact` in `useCareerFacts`, `ImpactBanner` (affected drafts, diff, link; submitted snapshots untouched), analyses marked stale by facts revision | `careerFacts.test.ts` (impact), `careerFit.test.ts` (staleness) | Live (QA account, synthetic fixture: one draft cover letter and one submitted snapshot referencing the Northwind experience fact): editing the fact's description (40 % → 35 %) showed the impact banner "2 drafts were built from the previous version of this fact" with the before/after diff, the draft with "Review draft" and the snapshot labelled "submitted snapshot — unchanged"; database: draft `stale = true` (revision 2), snapshot untouched (revision 1, `stale = false`), fit analysis `stale = true`, fact revision 3, profile `facts_revision` bumped, event `career_claim_corrected {affected: 2, revision: 3}` (ids/counts only). "Review draft" opened the application's Cover letter section labelled "Draft · Out of date"; saving new text and "Mark reviewed" cleared the stale flag (revision 4/5) while the snapshot stayed identical. |
| COS-024 Evidence-based interview preparation | `interviewRepo.ts`, `interviewApi.ts` client, `ai-interview` function (questions/feedback, abstain), `InterviewSection` (zone-aware time, themes from requirements, story candidates from confirmed facts) | `interviewApi.test.ts` 3, ai-interview Deno, `application.test.tsx` | J02 step 7: session in `Europe/Berlin`, themes from 6 requirements, stories from confirmed facts, questions → abstain uncharged; Today ranked "Prepare for your … interview" with the recorded time (formatted, not ISO). |
| COS-025 Submission, responses and outcomes with provenance | `outcomeRepo.ts`, `outcomes.ts` (`applyOutcomeToStage`, `effectiveOutcomes`, `unknownResponses`), `SubmissionDialog`, `OutcomeDialog` | `campaignReadinessOutcomes.test.ts`, `frontier.test.ts` (corrections) | J02 steps 8–9: `submitted_at`, `submission_snapshot {method}`, stage/legacy trigger, `submitted` outcome `user_reported`; interview scheduled → stage `interview`; "Open employer site" never changes stage. |
| COS-026 Typed Coach gateway | see [COS-026-gateway.md](COS-026-gateway.md) | career-gateway Deno, `gateway.test.ts` 8 | Agent live script on the local runtime: idempotent `start_application` (same run/request id), `stale_context` 409, unknown tool 400, foreign id 404, `record_outcome` confirmation token with `confirmation_mismatch` on edited content then completion, `prepare_interview`, `cancel_run`, RLS insert denied (42501). |
| COS-027 One persistent contextual Coach | `career-coach` function (bounded context, grounding, abstain), `coachApi.ts`, `CoachSpace` + `coach/*` (thread, context panel, memory menu, agent suggestions, tool results) | `coach.test.tsx` 13, `coachApi.test.ts` 3, career-coach Deno | J02 step 10: scoped conversation with locked relations; stored user + abstained assistant rows; `released: true`; StrictMode scoping fix. |
| COS-028 Library and asset relationships | `LibrarySpace` (CVs → existing `ResumeManager`/builder, reports, headshots, cover letters, stories, evidence), `DocumentDetail`, `LibraryDialogs`, `TemplateGallery`, `useLibraryAssets` | `library.test.tsx` 9, `templateGallery.test.ts` 4, export suites (`exportPdf` 7, `resumeDocx` 4, `saveFile` 6) | Native iOS build listed the real assets (2 CVs, 10 evidence items); template gallery reached via the legacy redirect; downloads go through the existing `saveFile`. |
| COS-029 Command search and actionable inbox | `CommandPalette` (Cmd/Ctrl-K, grouped owned results, keyboard-only), `SearchSpace`, `NotificationsSpace`/`InboxList`, `notificationRepo.ts` (dedupe, read/dismiss) | `search.test.tsx` 7, `inbox.test.tsx` 5, `repos.queue.test.ts` | J02 step 11: grouped results (Actions, Opportunities, Applications, Facts, Coach conversations); 2 action-required inbox rows with `actionId`; dismissed reminders never resurface. |
| COS-030 Resumable onboarding and existing-user introduction | `OnboardingFlow` + `useOnboardingState` (persisted step), `IntroductionCard`, anonymous claim (`anonymousClaims.ts`, `ClaimDraftDialog`) | `onboarding.test.tsx` 4, `draftCache.test.ts` 13, `persistenceLogic.test.ts` 7 | J01: objective → import → review (reload resumed) → goal → Today; J02: no forced onboarding, history reachable; J03: claim preview with Keep both / Replace primary / Discard. |

## Cross-cutting checks applied to every surface

* Ownership: every query goes through `useOwnedQuery`/repos with `user_id` filters and RLS; malformed ids raise `NotFoundError` before any query; two-owner reads blocked (migration smoke).
* Context: `resolveContext` keyed on the route subject; conflicting hints rewritten; account switch clears caches.
* States: loading, empty, populated, partial, error/retry, AI-unavailable, denied, offline and dark theme exercised per space (see COS-032/033).
* Localization: all new copy via `t('careeros.*')`; es/fr/de parity enforced by `translationCoverage.test.ts` (8).
* Export/deletion: 22 Career OS tables covered by `account-export` v2 and cascade delete (lifecycle smoke).

## Not claimed

Live model output quality, a screen-reader walkthrough, production cohorts, the hosted schema state and any real submission or message.
